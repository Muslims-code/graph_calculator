/// Represents an offset in a graph.
class GraphOffset {
  double x; // The x-coordinate of the offset.
  double y; // The y-coordinate of the offset.

  /// Creates a [GraphOffset] with the specified [x] and [y] coordinates.
  GraphOffset(this.x, this.y);
}
